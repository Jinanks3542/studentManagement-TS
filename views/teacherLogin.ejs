<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Teacher Login</title>
<style>
  body {
    margin: 0;
    padding: 0;
    height: 100vh;
    background: linear-gradient(135deg, #0f0f0f, #1a1a1a); /* Deep black gradient */
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .login-container {
    background: #1e1e1e; /* Dark card background */
    border-radius: 15px;
    padding: 40px;
    width: 100%;
    max-width: 420px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.6);
    color: #f1f1f1;
  }

  .login-container h2 {
    text-align: center;
    margin-bottom: 24px;
    color: #ffffff;
    font-size: 28px;
  }

  .form-group {
    margin-bottom: 20px;
  }

  label {
    display: block;
    margin-bottom: 8px;
    color: #ccc;
    font-weight: 500;
  }

  input[type="email"],
  input[type="password"] {
    width: 100%;
    padding: 12px;
    border: none;
    border-radius: 8px;
    font-size: 14px;
    background-color: #2a2a2a;
    color: #f1f1f1;
    box-shadow: inset 0 0 5px rgba(255, 255, 255, 0.05);
    transition: 0.3s ease;
  }

  input[type="email"]:focus,
  input[type="password"]:focus {
    outline: none;
    background-color: #333;
    box-shadow: 0 0 5px #4e9eff;
    border: 1px solid #4e9eff;
  }

  .login-btn {
    width: 100%;
    padding: 12px;
    background-color: #193467; /* Light blue button */
    color: white;
    font-size: 16px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .login-btn:hover {
    background-color: #0e183f;
  }

  .error-message {
    color: #ff6b6b;
    text-align: center;
    margin-top: 12px;
  }
</style>


</head>
<body>

  <div class="login-container">
    <h2>Teacher Login</h2>
    <form action="/loginTeacher" method="POST">
      <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required placeholder="Enter your email">
      </div>

      <div class="form-group">
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required placeholder="Enter your password">
      </div>

      <button type="submit" class="login-btn">Login</button>

      <% if (typeof error !== "undefined") { %>
        <div class="error-message"><%= error %></div>
      <% } %>
    </form>
  </div>

</body>
</html>